// Placeholder mínimo para poder probar el shell de (app) en el paso 2 — el contenido real
// (listado de reservas del usuario) se construye en el paso 6 en adelante (blueprint.md §9).
export default function MisReservasPage() {
  return <p className="text-muted-foreground">Aún no tienes reservas.</p>;
}
